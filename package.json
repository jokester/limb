{
  "name": "@jokester/limb",
  "version": "0.0.1",
  "description": "",
  "scripts": {
    "build": "rm -rf build && npm run --workspace=server build && npm run --workspace=demo build",
    "demo": "npm run --workspace=demo dev",
    "server": "npm run --workspace=server dev",
    "tsc": "tsc"
  },
  "workspaces": [
    "./server",
    "./demo"
  ],
  "devDependencies": {
    "gts": "^5.2.0",
    "typescript": "^5.3.3",
    "esbuild": "^0.20.0",
    "vite": "^5.0.12"
  },
  "author": "",
  "license": "ISC"
}
